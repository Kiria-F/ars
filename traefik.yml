entryPoints:
  web:
    address: :80
  websecure:
    address: :443

providers:
  docker:
    exposedByDefault: false
    
certificatesResolvers:
  letsEncrypt:
    acme:
      email: kiriakov.f.m@gmail.com
      storage: acme.json
      httpChallenge:
        entryPoint: web
    
api:
  dashboard: true