services:

  postgres:
    image: postgres
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: %DB_PASSWORD%
    ports:
      - 64205:5432
    volumes:
      - %DB_VOLUME%

  app:
    image: ars
    restart: on-failure
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 64208:8080

volumes:
  postgres-data: